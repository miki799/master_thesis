kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        apiVersion: kubeadm.k8s.io/v1beta3
        kind: ClusterConfiguration
        apiServer:
          extraArgs:
            audit-log-path: /var/log/kubernetes/kube-apiserver-audit.log
            audit-policy-file: /etc/kubernetes/policies/audit-policy.yaml
            admission-control-config-file: /etc/kubernetes/pss-config/pss-cluster.yaml
            encryption-provider-config: /etc/kubernetes/enc/encryption-configuration.yaml
          # mount new files / directories on the control plane
          extraVolumes:
            - name: audit-policies
              hostPath: /etc/kubernetes/policies
              mountPath: /etc/kubernetes/policies
              readOnly: true
              pathType: DirectoryOrCreate
            - name: audit-logs
              hostPath: /var/log/kubernetes
              mountPath: /var/log/kubernetes
              readOnly: false
              pathType: DirectoryOrCreate
            - name: pss-config
              hostPath: /etc/kubernetes/pss-config
              mountPath: /etc/kubernetes/pss-config
              readOnly: true
              pathType: DirectoryOrCreate
            - name: enc-config
              hostPath: /etc/kubernetes/enc
              mountPath: /etc/kubernetes/enc
              readOnly: true
              pathType: DirectoryOrCreate
    # mount the local files on the control plane
    extraMounts:
      - hostPath: ./src/k8s/secured_app/audit-policy/audit-policy.yaml
        containerPath: /etc/kubernetes/policies/audit-policy.yaml
        readOnly: true
        propagation: None # changes on the host system won't be propagated to the container
      - hostPath: ./src/k8s/secured_app/pod-security-standard/pss-cluster.yaml
        containerPath: /etc/kubernetes/pss-config/pss-cluster.yaml
        readOnly: true
        propagation: None
      - hostPath: ./src/k8s/secured_app/api-server/encryption-configuration.yaml
        containerPath: /etc/kubernetes/enc/encryption-configuration.yaml
        readOnly: true
        propagation: None
  - role: worker
networking:
  # disables kindnet CNI which doesn't support NetworkPolicies
  disableDefaultCNI: true
